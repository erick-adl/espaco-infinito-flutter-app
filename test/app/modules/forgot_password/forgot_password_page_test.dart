import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:infinito/app/modules/forgot_password/forgot_password_page.dart';

main() {
  testWidgets('ForgotPasswordPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ForgotPasswordPage(title: 'ForgotPassword')));
    //  final titleFinder = find.text('ForgotPassword');
    //  expect(titleFinder, findsOneWidget);
  });
}
