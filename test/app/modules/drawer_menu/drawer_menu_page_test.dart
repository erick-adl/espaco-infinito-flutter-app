import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:infinito/app/modules/drawer_menu/drawer_menu_page.dart';

main() {
  testWidgets('DrawerMenuPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DrawerMenuPage(title: 'DrawerMenu')));
    //  final titleFinder = find.text('DrawerMenu');
    //  expect(titleFinder, findsOneWidget);
  });
}
