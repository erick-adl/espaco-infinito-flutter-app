import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:infinito/app/modules/modules_controller.dart';
import 'package:infinito/app/modules/modules_module.dart';

void main() {
  initModule(ModulesModule());
  // ModulesController modules;
  //
  setUp(() {
    //     modules = ModulesModule.to.get<ModulesController>();
  });

  group('ModulesController Test', () {
    //   test("First Test", () {
    //     expect(modules, isInstanceOf<ModulesController>());
    //   });

    //   test("Set Value", () {
    //     expect(modules.value, equals(0));
    //     modules.increment();
    //     expect(modules.value, equals(1));
    //   });
  });
}
