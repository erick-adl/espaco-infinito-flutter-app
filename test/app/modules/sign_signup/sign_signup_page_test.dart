import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:infinito/app/modules/sign_signup/sign_signup_page.dart';

main() {
  testWidgets('SignSignupPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SignSignupPage(title: 'SignSignup')));
    //  final titleFinder = find.text('SignSignup');
    //  expect(titleFinder, findsOneWidget);
  });
}
