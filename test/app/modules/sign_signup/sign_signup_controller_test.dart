import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:infinito/app/modules/sign_signup/sign_signup_controller.dart';
import 'package:infinito/app/modules/sign_signup/sign_signup_module.dart';

void main() {
  initModule(SignSignupModule());
  // SignSignupController signsignup;
  //
  setUp(() {
    //     signsignup = SignSignupModule.to.get<SignSignupController>();
  });

  group('SignSignupController Test', () {
    //   test("First Test", () {
    //     expect(signsignup, isInstanceOf<SignSignupController>());
    //   });

    //   test("Set Value", () {
    //     expect(signsignup.value, equals(0));
    //     signsignup.increment();
    //     expect(signsignup.value, equals(1));
    //   });
  });
}
